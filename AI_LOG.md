- [1. Инструменты и раскладка](#1-инструменты-и-раскладка)
- [2. Ключевые скиллы и промпты](#2-ключевые-скиллы-и-промпты)
  - [2.1. Skills (переиспользуемая библиотека промптов)](#21-skills-переиспользуемая-библиотека-промптов)
    - [2.1.1. Уточняющие вопросы заказчику](#211-уточняющие-вопросы-заказчику)
    - [2.1.2. Задача для таск трекера](#212-задача-для-таск-трекера)
    - [2.1.3. Выявление дефектов в данных](#213-выявление-дефектов-в-данных)
    - [2.1.4. Ревью проекта](#214-ревью-проекта)
  - [2.2. Prompts (разовые уникальные задачи или запросы, которые еще не отточены для скиллов):](#22-prompts-разовые-уникальные-задачи-или-запросы-которые-еще-не-отточены-для-скиллов)
    - [2.2.1. Проектирование аналитического решения](#221-проектирование-аналитического-решения)
    - [2.2.2. SQL](#222-sql)
    - [2.2.3. ТЗ на загрузку из SRM API](#223-тз-на-загрузку-из-srm-api)
- [3. Ошибки модели](#3-ошибки-модели)
- [4. Что делалось руками и почему](#4-что-делалось-руками-и-почему)
- [5. Оценка времени](#5-оценка-времени)


# 1. Инструменты и раскладка
- Claude Code (для прикладной работы)
    - Model: Opus 5
- ChatGPT (для подготовки скиллов, промптов, проверки)

Для минимизации ошибок основной модели рабочий процесс проводился в несколько этапов:
1. Использовалась вспомогательная модель для составления промптов/скиллов.
2. Выполнялась работа основной моделью.
3. Запускалось ревью для проверки всего проекта.
4. Исправлялись ошибки в проекте на основании ревью.
5. Проводилась ручная валидация особо важных артефактов (договоренности, предложения, расчеты) 

# 2. Ключевые скиллы и промпты
## 2.1. Skills (переиспользуемая библиотека промптов)
### 2.1.1. Уточняющие вопросы заказчику

name: presale-questions
description: Генерирует список уточняющих вопросов клиенту по описанию бизнес-фичи для пресейла
argument-hint: [путь-к-файлу-с-фичей]
disable-model-invocation: true


Ты — Solution / Presale архитектор уровня senior.
Прочитай описание фичи из файла $ARGUMENTS.
Напиши уточняющие вопросы, которые необходимо задать заказчику, чтобы составить верхнеуровневую смету и КП.


1. Не задавай вопросы, ответ на которые уже есть в описании.
2. Не пиши слишком много вопросов. Достачточно 10 - 15, но информативных. В детали не уходи
3. Если в файле видишь результат беседы с несколькими представителями заказчика, то при составлении вопросов указывай: кому лично направлен вопрос
4. Сгруппируй вопросы по темам
5. Для каждого вопроса указывай, почему важно и что именно заблокируется, если ответа не будет
6. Сохрани результат в файл questions.md рядом с исходным файлом, в виде нумерованного списка.

---
### 2.1.2. Задача для таск трекера
name: dev-task

description: Оформляет одну задачу из согласованного scope в компактном формате для разработки без дополнительных уточнений

argument-hint: [название-задачи]

disable-model-invocation: true


Ты — Technical Project Manager / Systems Analyst.

Тебе необходимо оформить одну задачу для разработчика в формате Jira.

Название и информация о задаче передаётся в файле $ARGUMENTS.

Ничего не меняй в проекте! Только, составь задачу и сохрани ее в файле, который будет указан в файле $ARGUMENTS.

**Цель**

Разработчик должен иметь достаточно информации, чтобы реализовать задачу без дополнительных уточняющих вопросов.

При этом задача должна оставаться компактной и пригодной для размещения в Jira.

Не превращай постановку в отдельное техническое задание или проектную документацию.

Ссылайся на существующие артефакты проекта, если детальная информация уже описана там.

**Формат**

**Контекст**

2–4 предложения:

- зачем нужна задача;
- какой бизнес-результат она поддерживает.

**Что нужно сделать**

Краткий список конкретных результатов разработки.

**Границы**

**Входит**

Что реализуется в этой задаче.

**Не входит**

Что сознательно исключено.

**Входные данные**

Какие источники, таблицы или события использует задача.

**Результат**

Что должно появиться после реализации:

- таблица;
- витрина;
- API;
- процесс;
- другой конкретный артефакт.

Укажи название результата, если оно уже определено в модели.

**Правила реализации**

Только ключевые бизнес- и технические правила, без которых разработчик может реализовать задачу неправильно.

Не дублируй подробности из существующей документации.

**Критерии приёмки**

Используй проверяемые критерии в формате Given / When / Then или эквивалентном.

Добавь только существенные критерии.

**Тест-кейсы**

Включи:

- минимум 2 позитивных сценария;
- минимум 2 негативных или граничных сценария.

Используй реальные особенности и дефекты датасета, если они релевантны задаче.

**Definition of Done**

Краткий список:

- код реализован;
- тесты проходят;
- критерии приёмки выполнены;
- обработаны ошибки и граничные случаи;
- результат доступен для дальнейшего использования.

Не добавляй формальные пункты, не связанные с задачей.

**Требования к стилю**

1. Пиши кратко и конкретно.

2. Не используй общие формулировки вроде:
   «сделать качественно»,
   «обеспечить корректную работу»,
   «учесть все ошибки».

3. Каждый критерий должен быть проверяемым.

4. Не описывай архитектуру всего проекта.

5. Не дублируй содержание ТЗ и других документов.

6. Если важная деталь уже описана в другом артефакте, дай ссылку на него вместо копирования.

7. Не придумывай новые требования.

8. Итоговая задача должна помещаться в одну стандартную Jira issue и быть удобной для чтения.

**Финальная проверка**

Перед сохранением проверь:

- задача имеет один понятный результат;
- разработчик понимает вход и выход;
- границы задачи очевидны;
- критерии приёмки тестируемы;
- есть негативные сценарии;
- нет лишнего дублирования проектной документации;
- постановка не выглядит как ТЗ на десятки страниц.

--- 
### 2.1.3. Выявление дефектов в данных

name: data-quality-defects

description: Проводит аудит файлов датасета, выявляет реальные дефекты данных, подтверждает их проверками и описывает влияние на аналитику и обработку в пайплайне

argument-hint: [путь-к-папке-с-датасетом]

disable-model-invocation: true

Ты — Senior Data Engineer / Analytics Engineer с опытом построения аналитических хранилищ и data pipelines.

Тебе передана папка с исходными данными проекта: $ARGUMENTS.

Твоя задача — провести системный аудит всех файлов в папке, найти реальные дефекты данных и подготовить материал для раздела «Что не так с данными».

Исходные данные могут содержать намеренно заложенные ошибки и ловушки.

**Главный принцип**

Не предполагай наличие дефекта без проверки.

Не пиши «в данных могут быть дубликаты» или «возможны проблемы с датами».

Каждый дефект, включённый в итоговый результат, должен быть подтверждён фактическими данными, SQL-запросом, Python-проверкой или другой воспроизводимой проверкой.

Потенциальные проблемы, которые не удалось подтвердить, не включай в основной список дефектов.

---

**Этап 1. Инвентаризация файлов**

Сначала:

1. Найди все файлы в переданной папке и её подпапках.

2. Для каждого табличного файла определи:

   - имя файла;
   - формат;
   - количество строк;
   - список колонок;
   - предполагаемые типы данных;
   - количество NULL по каждой колонке;
   - количество уникальных значений;
   - возможные ключевые поля.

3. Определи вероятные связи между файлами.

4. Не изменяй исходные файлы.

5. Все проверки выполняй только в режиме чтения.

---

**Этап 2. Системный Data Profiling**

Проверь данные по следующим направлениям.

**1. Структурные проблемы**

- дубликаты строк;
- дубликаты предполагаемых бизнес-ключей;
- отсутствующие обязательные значения;
- некорректные типы данных;
- значения, не соответствующие ожидаемому формату.

**Referential Integrity**

Проверь предполагаемые связи между таблицами:

- записи, ссылающиеся на отсутствующие сущности;
- строки заказов без соответствующего заказа;
- факты без соответствующих справочников;
- неизвестные поставщики;
- неизвестная номенклатура;
- неизвестные единицы измерения;
- другие нарушения связей, которые можно вывести из структуры данных.

Не считай нарушение ссылочной целостности дефектом автоматически. Сначала проверь, действительно ли связь предполагается логикой датасета.

**Временные данные**

Проверь:

- некорректные даты;
- невозможную последовательность событий;
- даты в будущем относительно периода датасета;
- даты до логически возможного события;
- пересечения периодов историчности;
- gaps и overlaps в SCD-истории;
- несколько одновременно актуальных записей;
- timezone inconsistencies, если в данных используются разные временные зоны.

**Количественные значения**

Проверь:

- отрицательные значения там, где они не ожидаются;
- нулевые значения;
- экстремальные выбросы;
- значения, нарушающие бизнес-логику;
- полученное количество больше заказанного;
- невозможные коэффициенты пересчёта;
- другие количественные аномалии.

Не считай любой выброс ошибкой автоматически. Убедись, что он действительно выглядит как дефект, а не как допустимое редкое бизнес-событие.

**Цены и суммы**

Проверь:

- отрицательные и нулевые цены;
- несоответствие цены и суммы;
- резкие аномальные отклонения;
- проблемы валют;
- отсутствие необходимых валютных курсов;
- дублирование или пропуски курсов;
- логические несоответствия между ценами в связанных таблицах.

**Справочники**

Особенно внимательно проверь:

- историчность поставщиков;
- дублирование поставщиков;
- несколько current-записей;
- пересекающиеся периоды valid_from / valid_to;
- некорректные или отсутствующие связи номенклатуры;
- единицы измерения и коэффициенты пересчёта.

**Событийные данные**

Если в датасете есть факты событий:

- проверь дубли событий;
- повторную доставку;
- события вне хронологического порядка;
- отсутствие идентификаторов;
- несоответствие события исходной сущности;
- противоречивые статусы.

---

**Этап 3. Анализ влияния на аналитику**

Для каждого найденного дефекта ответь не только на вопрос «что сломано», но и:

1. Какие конкретные метрики могут исказиться?

2. В какую сторону может произойти искажение:
   - завышение;
   - занижение;
   - двойной учёт;
   - потеря данных;
   - некорректное распределение между поставщиками / категориями / периодами;
   - невозможность расчёта.

3. Может ли дефект привести к неверным управленческим выводам?

4. Какие витрины или дашборды особенно пострадают?

Не используй абстрактные формулировки вроде:

«Может повлиять на качество аналитики».

Указывай конкретно, например:

«Дубли приёмок приведут к завышению фактически полученного количества и искусственному росту показателя In Full».

---

**Этап 4. Проектирование обработки в pipeline**

Для каждого дефекта предложи конкретный способ обработки.

Возможные варианты:

- validation rule;
- дедупликация;
- quarantine / reject;
- исправление на staging;
- нормализация;
- mapping;
- сохранение исходного значения и вычисление нормализованного;
- SCD validation;
- data quality alert;
- reconciliation;
- загрузка в Unknown member;
- ручной reference mapping;
- исключение из конкретной метрики;
- отдельный бизнес-правило обработки.

Не применяй один и тот же шаблон ко всем дефектам.

Выбирай обработку исходя из природы конкретной проблемы.

Обязательно различай:

- что исправляется автоматически;
- что требует ручного разбора;
- что должно блокировать загрузку;
- что можно пропустить, но нужно мониторить.

---

**Этап 5. Отбор дефектов**

После полного анализа выбери минимум 5 наиболее значимых и доказуемых дефектов.

Приоритет отдавай дефектам, которые:

1. Реально присутствуют в данных.

2. Могут существенно повлиять на бизнес-метрики.

3. Демонстрируют понимание data engineering, а не только поверхностный profiling.

4. Релевантны аналитическим задачам проекта.

Если найдено больше 5 качественных дефектов, можешь включить до 10, отсортировав их по значимости.

Не включай несколько вариаций одной и той же проблемы как отдельные дефекты.

Например:

- NULL supplier_id;
- supplier_id отсутствует в справочнике;
- supplier_id имеет неверный формат

не должны автоматически становиться тремя отдельными дефектами, если это проявления одной корневой проблемы.

---

**Формат результата**

Сохрани результат в файл data_defects.md рядом с папкой датасета.

Начни файл с краткого раздела:

**Резюме анализа**

Укажи:

- какие файлы были проверены;
- сколько строк данных было проанализировано;
- сколько значимых дефектов найдено;
- какие области данных оказались наиболее проблемными.

Затем создай основной раздел:

**Найденные дефекты данных**

Для каждого дефекта используй строго следующую структуру.

**Дефект N. Краткое название**

**В чём дефект**

Чётко опиши фактическую проблему.

Приведи конкретные признаки:

- количество затронутых строк;
- примеры значений;
- процент от общего объёма, если это уместно.

**Как обнаружили**

Опиши конкретную проверку.

Если использовался SQL, Python или другой код:

- укажи логику проверки;
- сохрани запрос или скрипт проверки;
- сделай проверку воспроизводимой.

**Как влияет на цифры в дашборде**

Укажи:

- конкретные метрики;
- характер искажения;
- возможное бизнес-последствие.

**Как обрабатываем в пайплайне**

Опиши:

- на каком слое пайплайна обнаруживается проблема;
- правило обработки;
- что происходит с некорректной записью;
- нужен ли alert или ручной разбор.

**Приоритет**

Высокий / Средний / Низкий.

---

В конце добавь итоговую таблицу:

| № | Дефект | Затронуто данных | Влияние | Приоритет | Способ обработки |
|---|---|---:|---|---|---|

---

**Дополнительные требования**

1. Не исправляй и не перезаписывай исходные данные.

2. Не генерируй искусственные примеры вместо реальных дефектов.

3. Не выдавай потенциальные риски за обнаруженные факты.

4. Если для проверки требуется предположение о бизнес-правиле, явно помечай его как предположение.

5. Если данных недостаточно, чтобы признать что-то дефектом, зафиксируй это как наблюдение, но не включай в основной список.

6. При обнаружении подозрительной аномалии сначала попытайся проверить её через связанные таблицы.

7. Старайся искать не только очевидные технические ошибки, но и дефекты, которые приводят к правдоподобным, но неправильным бизнес-метрикам.

8. Особое внимание удели дефектам, которые могут остаться незаметными при поверхностном анализе и привести к убедительно выглядящим, но неверным цифрам.


---
### 2.1.4. Ревью проекта
name: generate-erd
description: Проверяет выполненное задание по проекту на соответствие требованиям
argument-hint: [папка-проекта]
disable-model-invocation: true

Ты — независимый Senior Data Architect и технический reviewer.

Проведи adversarial review выполненного задания.

Твоя задача НЕ улучшить документы и НЕ переписать их.

Твоя задача — найти случаи, где AI мог выдать правдоподобное, профессионально звучащее, но неверное или необоснованное решение.

Все доступные артефакты проекта описаны в файле $ARGUMENTS:

Особенно ищи следующие типы ошибок:

1. Решение выглядит технически правильно, но противоречит исходным данным.
2. Сделано неподтверждённое допущение.
3. Модель перепутала scope текущего и будущего этапа.
4. Метрика определена корректно текстово, но SQL реализует её иначе.
5. JOIN, агрегация или оконная функция создают незаметное искажение цифр.
6. Архитектурное решение предполагает возможности API или источника, которые не подтверждены.
7. Ответ слишком категоричен при недостатке информации.
8. Предложенное решение невозможно реализовать при заданных ограничениях.
9. Результат формально отвечает на вопрос, но пропускает важное условие задания.
10. Не учтены найденные дефекты данны при последующих расчетах и проектировании.

Не придумывай ошибки.

Если решение корректно — не называй его ошибкой.

Для каждой найденной проблемы укажи:

- файл и место;
- что именно было предложено;
- почему это выглядело убедительно;
- почему это неверно или рискованно;
- как это можно проверить;
- критичность: высокая / средняя / низкая.

Найди до 10 кандидатов, но включай только реальные и аргументированные проблемы.

В конце выдели 3 наиболее сильных кандидата для Блока 8.

Не исправляй документы автоматически.
Сначала только подготовь отчёт.

Сохрани результат в файл (название указано в файле $ARGUMENTS).

## 2.2. Prompts (разовые уникальные задачи или запросы, которые еще не отточены для скиллов):
### 2.2.1. Проектирование аналитического решения

Ты — Senior Data Architect / Analytics Engineer с опытом проектирования аналитических платформ и data warehouse для закупочной аналитики.

**Входные материалы**

Перед началом работы изучи следующие материалы:

1. В файле `01_discovery.md`: 
   - Конспект стартовой встречи;
   - Scope первого релиза;

2. Результаты анализа качества исходных данных и найденные дефекты:
   `data_defects.md`

3. Исходные данные проекта:
   папка `data/`

Используй все материалы как единый контекст.

Не игнорируй реальные особенности и дефекты данных, найденные при profiling. Архитектурные решения должны учитывать их там, где они влияют на модель, витрины, ключи, историчность или расчёт метрик.

**Границы проектирования**

Проектируй решение прежде всего для scope первого релиза.

Не проектируй избыточную корпоративную платформу и не создавай таблицы только ради потенциальных будущих функций, не входящих в первый релиз.

При этом модель не должна создавать архитектурный тупик для очевидных следующих этапов проекта. Если источник или сущность необходимы для естественного развития решения, но не реализуются в первом релизе, достаточно предусмотреть расширяемость модели, не включая лишнюю реализацию в текущий DDL.

Не расширяй scope самостоятельно.

**Важный принцип**

Различай:

- исходные таблицы источников;
- staging / нормализованные данные;
- целевую аналитическую модель;
- аналитические витрины.

Не копируй структуру CSV-файлов механически в целевую модель.

Проектируй модель исходя из бизнес-вопросов первого релиза и реального grain данных.

---

**Необходимо выполнить**

**2.1. ER-диаграмма целевой модели**

Спроектируй целевую аналитическую модель.

Выбери подходящую схему (например, star schema, snowflake или другой вариант) и кратко обоснуй выбор.

Создай ER-диаграмму в Mermaid.

Диаграмма должна показывать:

- фактовые таблицы;
- измерения;
- ключевые связи между ними;
- основные ключи.

Не перегружай диаграмму техническими деталями, несущественными для понимания модели.

Сохрани Mermaid-диаграмму также отдельным блоком в итоговом документе, чтобы её можно было при необходимости вынести в отдельный `02_model_diagram.mmd` файл.

---

**2.2. Зерно таблиц**

Для каждой фактовой таблицы и аналитической витрины явно укажи зерно.

Используй формулировку:

> Одна строка = ...

Зерно должно быть конкретным и однозначным.

Особенно внимательно проверь риск двойного учёта при соединении:

- заказов;
- строк заказов;
- приёмок;
- исторических версий поставщиков;
- справочников.

Не допускай ситуации, когда модель или витрина может неявно изменить grain и задвоить бизнес-факты.

---

**2.3. DDL целевых таблиц**

Подготовь сокращённый, но реалистичный DDL для PostgreSQL.

Для каждой ключевой таблицы укажи:

- название;
- основные поля;
- типы данных;
- primary key;
- foreign keys там, где они необходимы;
- business keys;
- ключевые unique constraints;
- необходимые индексы.

Не нужно писать полный production-ready DDL со всеми техническими полями и настройками.

DDL должен быть достаточным, чтобы разработчик понимал физическую структуру целевой модели.

Учитывай реальные дефекты данных при выборе ключей и ограничений.

Не накладывай ограничения, которые приведут к невозможности загрузки реально существующих проблемных данных без предусмотренного механизма обработки.

---

**2.4. Обработка специальных случаев**

Отдельно опиши архитектурное решение для следующих ситуаций:

**Историчность поставщика**

Как обрабатываются:

- переименования;
- изменение класса надёжности;
- исторические версии.

Выбери тип SCD и объясни почему.

Решение должно учитывать реальные особенности suppliers из датасета и найденные дефекты.

**Мультивалютность**

Опиши:

- какие суммы и валюты хранятся;
- где происходит конвертация;
- какой курс используется;
- как обеспечивается воспроизводимость исторических расчётов.

Не придумывай бизнес-правило выбора даты курса, если его нет во входных материалах. Если необходимо допущение — явно зафиксируй его.

**Разные единицы измерения**

Опиши, как нормализуются единицы измерения для корректного сравнения цен.

Учти реальные коэффициенты и проблемы справочника UOM, если они были найдены.

**Отмены и изменения строк заказа**

Опиши:

- что считается текущим состоянием;
- как учитываются изменения;
- как отмены влияют на аналитические факты;
- как избежать двойного учёта.

**Позиции без справочника номенклатуры**

Опиши, как сохраняется факт закупки и одновременно фиксируется проблема качества данных.

---

**2.5. Матрица метрик**

Создай таблицу:

| Метрика | Бизнес-определение | Зерно | Источники | Формула | Подводные камни |
|---|---|---|---|---|---|

Добавь минимум 8 метрик.

Метрики должны быть релевантны:

- бизнес-потребностям из стартовой встречи;
- scope первого релиза;
- доступным данным.

Не добавляй метрики только для выполнения количественного требования.

Обязательно включи OTIF, даже если он не реализуется в первом релизе, поскольку этого требует полное задание.

Для OTIF:

1. Предложи однозначное каноническое определение.

2. Явно опиши grain расчёта.

3. Объясни, какие условия определяют:
   - On Time;
   - In Full.

4. Разбери возможные причины расхождения между цифрами 94% и 71%, упомянутыми на встрече.

Не утверждай как факт неизвестную методику текущих расчётов подразделений.

Вместо этого сформулируй обоснованные гипотезы о различии методик и укажи, что для окончательного reconciliation требуется подтверждение бизнес-правил заказчиком.

5. Покажи, как предложенная каноническая методика позволит устранить спор.

---

**Требования к качеству решения**

1. Все архитектурные решения должны быть связаны с конкретными требованиями или особенностями проекта.

2. Не используй шаблонные enterprise-решения без необходимости.

3. Не добавляй технологии, которых нет в задании, если они не нужны для объяснения модели.

4. Если информации недостаточно для однозначного архитектурного решения, не выдумывай факт. Явно сформулируй допущение.

5. Разделяй:
   - факт из исходных материалов;
   - архитектурное решение;
   - допущение.

6. Учитывай найденные дефекты данных, но не превращай весь документ в повторный data profiling.

7. Следи за согласованностью между:
   - scope первого релиза;
   - ER-моделью;
   - grain таблиц;
   - DDL;
   - правилами обработки данных;
   - метриками.

8. Не проектируй сущности для функций, сознательно исключённых из scope первого релиза, если они не являются необходимым фундаментом текущей модели.

9. Перед финализацией выполни внутреннюю проверку:

   - соответствует ли модель scope;
   - нет ли таблиц без бизнес-потребности;
   - все ли связи сохраняют корректный grain;
   - возможен ли расчёт заявленных метрик;
   - не приведут ли joins к двойному учёту;
   - учтены ли существенные реальные дефекты данных;
   - сможет ли модель расширяться на следующий этап без полной переделки.

---

**Формат результата**

Сохрани готовый результат в файл:

`02_model.md`

Структура файла:

**2. Проектирование аналитического решения**

**2.1. Целевая модель и выбор схемы**

**Архитектурный подход**

**Обоснование выбора**

**ER-диаграмма**

[Mermaid diagram]

---

**2.2. Зерно таблиц и витрин**

**[Название таблицы]**

Одна строка = ...

[повторить для всех ключевых таблиц и витрин]

---

**2.3. DDL целевых таблиц**

[DDL PostgreSQL]

---

**2.4. Обработка сложных случаев**

**Историчность поставщика**

**Мультивалютность**

**Единицы измерения**

**Изменения и отмены заказов**

**Отсутствующая номенклатура**

---

**2.5. Матрица метрик**

[таблица]

**Каноническое определение OTIF**

**Почему сейчас могут получаться 94% и 71%**

**Предлагаемый способ reconciliation**

---

Пиши результат как документ, который будет читать технический интервьюер и руководитель проекта.

Приоритет — обоснованность решений, внутреннее отсутствие противоречий и возможность защитить каждое решение на интервью, а не объём текста.

---
### 2.2.2. SQL
Ты — Senior Data Engineer и PostgreSQL-разработчик.

Необходимо выполнить Блок 3 «SQL» задания из TS.docx.

В проекте уже выполнены предыдущие блоки. Используй их как обязательный источник истины.

**Входные материалы**

Перед началом работы изучи:
1. `TS.docx` - описание задания

2. `01_discovery.md` - согласованный Scope первого релиза, 

3. `02_model.md` - целевая модель, правила обработки данных и определения метрик.

2. `02_ddl.sql` - целевая физическая структура таблиц PostgreSQL.

3. `02_model_diagram.mmd` - ER-диаграмма.

4. Все файлы в папке `data/` - реальные исходные данные.


Не изменяй документы Блока 2.

**Задача**

Выполни Блок 3 задания из TS.docx.

PostgreSQL. Запросы должны реально отрабатывать на выданном датасете.

Не ограничивайся генерацией SQL «на глаз». Перед написанием запросов изучи реальные данные, названия колонок, типы и связи между файлами.

Используй определения и бизнес-правила из `02_model.md` как источник истины.

Если между моделью из Блока 2 и реальными исходными данными есть расхождение, не меняй молча определение метрики. Явно зафиксируй необходимое преобразование исходных данных для расчёта.

---

**Q1. OTIF по поставщикам**

Напиши PostgreSQL-запрос, который рассчитывает:

- OTIF по поставщикам за последние 6 полных месяцев;
- OTIF за весь шестимесячный период;
- динамику месяц-к-месяцу;
- ранг поставщика внутри его категории надёжности;
- только поставщиков с минимум 20 поставками за весь рассматриваемый период.

Определение OTIF должно строго соответствовать определению из `02_model.md`.

Добавь в начало SQL-файла комментарий с используемым определением OTIF.

Проверь:

- что последние 6 месяцев действительно полные;
- что текущий неполный месяц не включается;
- что grain расчёта соответствует Блоку 2;
- что JOIN не приводит к двойному учёту;
- что порог ≥20 поставок считается за весь период, а не отдельно по месяцам;
- что ранг считается внутри reliability category.

---

**Q2. Ценовые аномалии**

Напиши PostgreSQL-запрос:

Топ-15 позиций, где средневзвешенная цена за базовую единицу в последнем полном квартале отклоняется от медианной цены этой же позиции у других поставщиков более чем на 15%.

Вывести:

- позицию;
- категорию;
- поставщика;
- нашу средневзвешенную цену;
- медианную цену у других поставщиков;
- отклонение в процентах;
- сумму закупки.

Все значения должны быть приведены к рублям.

Проверь:

- что используется последний полный квартал;
- что цена действительно средневзвешенная;
- что сравнение происходит за базовую единицу;
- что учитывается нормализация UOM;
- что используется валютная конвертация согласно правилам Блока 2;
- что текущий поставщик исключён из расчёта медианы «других поставщиков»;
- что используется именно медиана;
- что фильтр >15% применяется корректно;
- что Top-15 выбирается после выполнения бизнес-фильтров.

---

**Q3. Концентрация поставщиков**

Напиши PostgreSQL-запрос, который для каждой категории рассчитывает:

- оборот поставщика;
- долю поставщика в обороте категории;
- накопительную долю поставщиков по убыванию оборота;
- количество поставщиков, которые суммарно дают 80% оборота категории.

Проверь:

- что расчёт partitioned по категории;
- что поставщики отсортированы по убыванию оборота;
- что cumulative share считается оконной функцией;
- что граница 80% определяется корректно;
- что одинаковый поставщик не задваивается из-за JOIN.

---

**Общие требования к SQL**

Для всех трёх запросов:

1. Используй PostgreSQL-синтаксис.

2. Используй CTE вместо длинных вложенных подзапросов.

3. Используй оконные функции там, где они уместны.

4. Добавь комментарии к неочевидным решениям.

5. Не используй фиктивные таблицы или названия колонок, которых нет в реальных данных или целевой DDL.

6. Не пиши SQL, который выглядит правдоподобно, но невозможно выполнить.

7. Учитывай реальные дефекты данных и правила их обработки из Блока 2.

8. Предпочитай читаемость SQL чрезмерной оптимизации.

---

**Результаты**

Заполни следующие файлы:

`03_sql/q1_otif.sql`

`03_sql/q2_price_anomalies.sql`

`03_sql/q3_supplier_concentration.sql`

Каждый SQL-файл должен содержать только соответствующий запрос и необходимые комментарии.

В текущем окружении доступен PostgreSQL, выполни каждый запрос на реальных данных. PostgreSQL доступен локально. Подключение выполняется через psql с параметрами из .env. Пароль автоматически предоставляется через настроенный ~/.pgpass. Не запрашивай пароль и не сохраняй credentials в проекте.

После выполнения сохрани реальные результаты:

`03_sql/q1_result.csv`

`03_sql/q2_result.csv`

`03_sql/q3_result.csv`

Не создавай пустые файлы.

Если выполнить PostgreSQL в текущем окружении невозможно, всё равно создай SQL-файлы, но НЕ создавай фиктивные CSV с выдуманными результатами.

В этом случае создай:

`03_sql/EXECUTION_NOTES.md`

и укажи:

- какие таблицы необходимо создать;
- какие исходные данные необходимо загрузить;
- в каком порядке выполнять SQL;
- какие предположения были сделаны;
- какие части требуют фактической проверки после запуска PostgreSQL.

---

**Performance ×100**

Создай файл:

`03_sql/performance.md`

Отдельным разделом для Q1, Q2 и Q3 укажи:

- потенциальный bottleneck при росте объёма данных в 100 раз;
- почему он возникнет;
- что можно сделать для масштабирования.

Не ограничивайся общими фразами вроде «добавить индексы».

Укажи конкретные направления:

- индексы;
- partitioning;
- pre-aggregation;
- materialized views;
- incremental calculations;
- оптимизация оконных функций;
- оптимизация JOIN;
- другие релевантные меры.

---

**Финальная самопроверка**

Перед завершением проверь:

1. Все ли три SQL-файла созданы и не пустые.
2. Соответствуют ли запросы определениям Блока 2.
3. Используются ли реальные таблицы и колонки.
4. Нет ли очевидного double counting.
5. Выполняются ли все требования Q1.
6. Выполняются ли все требования Q2.
7. Выполняются ли все требования Q3.
8. Созданы ли CSV-результаты только при реальном выполнении запросов.
9. Нет ли пустых или фиктивных файлов.

В конце выведи краткий отчёт:

- какие файлы созданы;
- какие запросы были реально выполнены;
- какие результаты получены;
- что осталось проверить вручную.

---
**ТЗ на SRM**

Ты — Senior Solution Architect / Data Architect

С опытом проектирования интеграций, data pipelines, REST API и event-driven архитектуры.

Необходимо выполнить **Блок 4** из TS.docx: «Интеграции и пайплайн».

Работай как архитектор, который готовит технические требования для последующей реализации интеграций. Не считай, что все описываемые интеграции должны быть реализованы в рамках текущего MVP.

---

**Контекст проекта**

Перед началом работы обязательно изучи следующие материалы проекта:

1. `01_discovery.md`  
   — результаты discovery, требования заказчика, описание текущих систем, фрагменты SRM API и as-is события WMS, scope первого релиза и риски.

2. `TS.docx`  
   — исходное тестовое задание

3. `02_model.md`  
   — целевая аналитическая модель, правила обработки данных, специальные случаи и определения метрик.

4. `02_model_diagram.mmd`  
   — ER-диаграмма целевой модели.

5. `02_ddl.sql`  
   — физическая структура целевой аналитической модели.

6. SQL-файлы из папки `03_sql/`  
   — реализованные аналитические запросы и фактические требования к данным для расчёта показателей.

7. Исходные данные проекта: папка `data/`


Используй эти материалы как единый контекст.

Не меняй решения из предыдущих блоков. Если обнаружишь противоречие между предыдущими артефактами и требованиями Блока 4, явно зафиксируй его в `04_integration.md` и предложи минимальное согласованное решение.

---

**Важное разграничение scope**

Согласно согласованному scope:

* текущий первый релиз использует подтверждённый источник данных — реплику 1С:УТ;
* реализация интеграции с SRM API не входит в текущий релиз;
* реализация нового Kafka-контракта со стороны WMS также не входит в текущий релиз.

Однако в рамках текущего этапа необходимо подготовить технические требования и целевую архитектуру этих интеграций, чтобы снизить неопределённость последующих этапов.

Поэтому в документе явно разделяй:

1. Что реализуется сейчас.
2. Что проектируется и согласовывается сейчас, но реализуется позже.
3. Что является целевым состоянием после внедрения интеграций.

---
### 2.2.3. ТЗ на загрузку из SRM API

Подготовь техническое задание для команды, которая в будущем будет реализовывать ingestion данных из SRM API. 

Используй реальный фрагмент API из исходных материалов.

Не придумывай API-методы, параметры или гарантии, которых нет в предоставленном описании.

Если для реализации необходима информация, которой нет в API-контракте, явно укажи это как допущение или вопрос к владельцу API.

**1. Цель интеграции**

* какие сущности загружаются;
* для каких аналитических задач они нужны;
* в какие слои аналитической платформы попадают данные;
* какие целевые сущности из модели Блока 2 они потенциально обогащают.

**2. Общую схему загрузки**

Опиши поток:

SRM API → ingestion → raw → staging → validation → целевая модель / аналитические слои.

Укажи ответственность каждого этапа.

**3. Инкрементальную загрузку `/tenders`**

Отдельно и подробно ответь на главный вопрос задания:

**Как организовать инкрементальную загрузку `/tenders`, если:**
* endpoint не предоставляет `updatedFrom`;
* порядок записей не гарантирован.

Предложи технически реалистичный алгоритм.

Опиши:

* что хранится как checkpoint/state;
* как определяется окно повторного сканирования;
* как предотвращается потеря изменённых записей;
* как обнаруживаются новые записи;
* как обнаруживаются изменения существующих записей;
* как обрабатываются исчезнувшие или отменённые записи, если это применимо;
* как обеспечивается идемпотентность.

Не используй offset-based инкремент как единственное решение, если отсутствие гарантированного порядка делает его небезопасным.

Если решение требует периодического полного reconciliation scan, опиши:

* как часто он запускается;
* почему;
* как влияет на нагрузку API.

**4. Rate limit: 100 req/min при 180 тыс. записей**

Отдельно рассчитай и опиши стратегию загрузки.

Учти:

* лимит 100 запросов в минуту;
* около 180 000 записей;
* поштучный метод получения лотов.

Не ограничивайся фразой «использовать rate limiter».

Опиши:

* очередь запросов;
* ограничитель скорости;
* допустимую параллельность;
* retry policy;
* backoff;
* сохранение прогресса;
* возможность продолжения после остановки;
* оценку порядка времени полной загрузки при заданных ограничениях.

Проверь математическую реалистичность предлагаемого подхода.

Если полная первичная загрузка занимает длительное время, объясни, как это влияет на архитектуру и запуск.

**5. Аутентификация и протухший токен**

Опиши:

* где хранится токен;
* как определяется истечение срока;
* proactive refresh или refresh после 401;
* как избежать одновременного refresh несколькими worker;
* что происходит с запросами, которые получили authentication error;
* как обеспечивается безопасное повторное выполнение.

Не помещай реальные credentials в конфигурационные примеры.

**6. Падение загрузки на 130-й тысяче**

Опиши конкретный механизм восстановления.

Должно быть понятно:

* что уже считается успешно обработанным;
* где хранится checkpoint;
* какие записи можно безопасно повторить;
* как предотвращаются дубли;
* как процесс продолжает работу после рестарта;
* требуется ли повторно сканировать часть данных.

Не используй формулировку «перезапустить загрузку» без описания механизма resume.

**7. Ошибки и retry**

Опиши обработку:

* 429;
* 401 / 403;
* 5xx;
* timeout;
* network failure;
* некорректного payload;
* частичной недоступности API.

Раздели retryable и non-retryable ошибки.

**8. Контроль качества**

Опиши:

* reconciliation между API и загруженными данными;
* контроль количества записей;
* контроль пропусков;
* schema validation;
* quarantine для некорректных payload;
* метрики ingestion.

**9. Observability**

Опиши минимально необходимые:

* логи;
* метрики;
* алерты;
* статус последней успешной загрузки.

---
**Контракт события приёмки для Kafka**

В исходных материалах есть as-is событие приёмки WMS.

На его основе подготовь контракт, который необходимо затребовать у команды WMS для нормальной аналитической интеграции.

Не просто опиши идеальный Kafka-event абстрактно.

Сохрани бизнес-смысл as-is события, но приведи его к пригодному для production-интеграции контракту.

**1. Целевую JSON-схему события**

Опиши структуру события.

Включи необходимые поля для:

* уникальной идентификации события;
* идентификации бизнес-сущности;
* времени события;
* времени публикации;
* поставщика;
* документа / заказа / строки заказа, если это требуется исходным контекстом;
* номенклатуры;
* количества;
* единицы измерения;
* статуса или типа события;
* версии схемы.

Не добавляй бизнес-поля без обоснования.

Для каждого важного поля укажи:

* назначение;
* обязательность;
* тип;
* правила валидации.

JSON-схема должна быть достаточно конкретной, чтобы её можно было передать WMS-команде.

**2. Ключ партиционирования**

Выбери partition key.

Объясни:

* почему выбран именно этот ключ;
* какие события должны попадать в одну partition;
* какой порядок необходимо сохранять;
* как выбранный ключ помогает обработке изменений и дублей.

**3. Гарантию доставки**

Определи ожидаемую семантику доставки.

Например:
* at-least-once;
* exactly-once только при определённых условиях.

Не заявляй exactly-once без технического обоснования.

Опиши, как потребитель должен работать при выбранной гарантии.

**4. Идемпотентность**

Опиши:

* какой event_id используется;
* как определяется уникальность;
* где хранится информация об уже обработанных событиях;
* как долго хранится deduplication state;
* что происходит при повторной доставке.

**5. Out-of-order события**

Опиши:

* как определяется порядок;
* какие timestamp использовать;
* допускается ли позднее событие;
* что происходит, если событие пришло после более нового состояния;
* как пересчитывается аналитический факт.

Не предполагай, что Kafka сама гарантирует глобальный порядок.

**6. Дубли**

Опиши отдельный сценарий:

* одинаковое событие пришло повторно;
* одно бизнес-событие пришло с разными event_id;
* producer повторил публикацию после timeout.

**7. Версионирование схемы**

Опиши:

* где хранится schema version;
* правила backward compatibility;
* правила добавления и удаления полей;
* как потребитель обрабатывает новую версию;
* нужна ли схема registry и почему.

**8. Что делать сейчас, пока WMS не переделала формат**

**Это обязательная часть.**

Предложи временное решение для текущего as-is события.

Опиши:

* ingestion текущего формата;
* адаптер / normalizer;
* какие поля можно извлечь;
* какие данные невозможно получить;
* какие ограничения останутся;
* какие проверки качества нужны;
* как временное решение будет заменено новым контрактом без полной переделки downstream pipeline.

Чётко раздели:

**AS-IS adapter сейчас**  
и  
**целевой WMS contract позже**.

---
**Полная схема пайплайна**

Спроектируй целостную схему движения данных от источников до аналитических витрин.

Используй решения Блоков 2 и 3.

Схема должна включать как текущие источники, так и целевые будущие интеграции, но явно обозначать их статус.

**Источники**

Минимально рассмотри те источники, которые подтверждены исходными материалами:

* реплика 1С:УТ;
* SRM API;
* WMS / Kafka.

Не добавляй новые источники без необходимости.

**Слои**

Опиши логические слои:

* ingestion / landing;
* raw;
* staging / standardization;
* data quality;
* core / analytics model;
* marts / semantic layer.

Не обязательно использовать именно эти названия, если модель Блока 2 предполагает другое разделение.

Главное — чётко объяснить назначение каждого слоя.

**Витрины**

Свяжи конечные витрины с:

* целевой моделью Блока 2;
* бизнес-результатом первого релиза;
* аналитическими запросами из Блока 3.

**Периодичность**

Для каждого источника и ключевого этапа укажи:

* периодичность загрузки;
* почему выбрана именно она;
* допустимую задержку данных;
* какие процессы batch;
* какие потенциально event-driven.

Не придумывай real-time SLA, если его нет в требованиях.

**Оркестрация**

Опиши:

* какие процессы зависят друг от друга;
* порядок выполнения;
* контроль успешного завершения;
* retries;
* зависимости между задачами;
* backfill.

Можно описывать логически, без привязки к конкретному оркестратору, если технология не задана.

**Сбой ночной загрузки**

**Обязательно подробно опиши сценарий:**

Что происходит, если ночной pipeline завершился ошибкой.

Укажи:

1. Как определяется факт сбоя.
2. Какие данные остаются доступными.
3. Откатывается ли последняя успешная версия.
4. Можно ли частично публиковать данные.
5. Как происходит retry.
6. Когда требуется ручное вмешательство.
7. Как выполняется recovery.
8. Как предотвращается публикация частично обновлённых витрин.

Предпочитай подход, при котором потребитель либо видит последнюю согласованную версию данных, либо новую полностью собранную версию, а не частично обновлённое состояние.

**Как заказчик узнаёт, что данные неактуальны**

**Это обязательный вопрос задания.**

Опиши конкретный механизм data freshness visibility:

* timestamp последнего успешного обновления;
* статус витрины;
* freshness indicator на дашборде;
* alert ответственным;
* допустимый SLA freshness;
* поведение дашборда при устаревших данных.

Не ограничивайся фразой «отправить уведомление».

---

**Схема архитектуры**

Подготовь визуальную схему полного pipeline в Mermaid.

Она должна показывать:

Источники → ingestion → слои данных → трансформации → целевую аналитическую модель → витрины / дашборды.

Обозначь разными логическими группами:

* реализуемое в текущем релизе;
* проектируемое сейчас для будущей реализации;
* будущие источники.

Не используй цвета как единственный способ передать смысл, чтобы схема оставалась понятной в текстовом виде.

---

**Требования к качеству**

1. Не проектируй избыточную enterprise-архитектуру.

2. Не добавляй технологии только ради технологичности.

3. Все решения должны быть связаны с конкретными ограничениями из задания.

4. Не выдумывай гарантии API или Kafka, которых нет во входных материалах.

5. Явно различай:
   * факт из исходных материалов;
   * архитектурное решение;
   * допущение;
   * открытый вопрос.

6. Особое внимание удели failure scenarios и recovery.

7. Решения должны быть согласованы с моделью и scope из предыдущих блоков.

8. Не противоречь scope первого релиза.

9. Если интеграция не реализуется сейчас, это не означает, что её не нужно подробно спроектировать в рамках данного задания.

---

**Формат результата**

Создай папку:

`04_integrations/`

И сохрани результаты в следующие файлы:

| Файл | Содержание |
|------|------------|
| `04_integrations/04_srm_api_spec.md` | техническое задание на загрузку из SRM API |
| `04_integrations/04_wms_kafka_contract.md` | целевой контракт события приёмки и стратегия временного адаптера |
| `04_integrations/04_pipeline.md` | описание полного pipeline, периодичности, оркестрации, recovery и data freshness |
| `04_integrations/04_pipeline_diagram.mmd` | Mermaid-схема полного pipeline |

Каждый файл должен быть заполнен реальным содержимым.

Не создавай пустые файлы.

---

**Финальная самопроверка**

Перед завершением проверь:

**SRM**

* дан конкретный алгоритм инкремента без updatedFrom;
* учтён не гарантированный порядок;
* рассчитан и описан rate limit 100 req/min;
* учтены 180 тыс. записей;
* описана поштучная загрузка лотов;
* описан expired token;
* описано падение на 130-й тысяче;
* есть resume и идемпотентность.

**Kafka**

* есть конкретная JSON-схема;
* выбран partition key;
* определена delivery semantics;
* описана идемпотентность;
* описаны out-of-order;
* описаны дубли;
* описано schema versioning;
* есть временное решение для as-is формата.

**Pipeline**

* показаны все подтверждённые источники;
* есть слои;
* есть витрины;
* есть периодичность;
* есть оркестрация;
* описан ночной failure scenario;
* описан recovery;
* заказчик видит freshness данных.

---

**Итоговый отчёт**

В конце выведи краткий отчёт о созданных файлах и ключевых архитектурных решениях.

---

# 3. Ошибки модели
1. Уточняющие вопросы клиенту. Наблюдения, которые стоит проговорить сразу. Были слишком категоричные замечания в части использования естесственной речи (зависимость от инфраструктуры клиента). Также, модель предложила развернуть модель в On-prem, как вариант. Я подкорректировал, объяснив, что использование AI подразумевает открытую модель, а использование модели внутри закрытой системы не стал озвучивать, так как это выходит за рамки допустимого дедлайна.
2. Письмо в файле `06_letter.md` обещало директору объяснить расхождение 8–10%, которого проект не закрывает. Заказчику сообщалось, что после первого релиза расхождение между отчётом закупок и отчётом финансов перестанет быть необъяснимым. Но, расхождение существует между двумя системами, и финансовый источник в проекте отсутствует. Объяснить разницу с системой, к которой нет доступа, нельзя ни при какой прозрачности своей стороны. Исправлено: теперь обещание, что расхождение в 8–10% перестанет быть необъяснимым, заменено отдельным коротким абзацем, который говорит, что за 4 недели мы его не объясним, финансовой системы в проекте нет, и на чьей стороне лежит разница, следует выяснить.
3. В предложениях по развитию аккаунта декомпозиция 17% выдавалось за готовый механизм объяснения финансового расхождения. После очистки данных о закупках мы смогли объяснить расхождение в 17%. И модель решила, что если мы смогли объяснять эти 17%, значит у нас уже есть готовый механизм объяснения финансового расхождения (8–10%). Модель перепутала 2 разных расхождения. Исправлено: теперь предложение начинается с аудита правил на обеих сторонах, а состав автоматизации определяется по его итогам.

# 4. Что делалось руками и почему
**Письмо заказчику**
**Валидация scope на 4 недели**
**Запуск ревью по 3-м блокам**
**Запуск SQL запросов и сравнение результатов**

# 5. Оценка времени
